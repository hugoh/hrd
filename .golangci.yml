version: "2"
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - godoclint
    - gomodguard
    - noinlineerr
    - paralleltest
    - testpackage
    - wsl
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - err113
          - noctx
          - varnamelen
          - goconst
          - gosec
formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    - golines
