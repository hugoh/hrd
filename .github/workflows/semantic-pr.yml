# DO NOT EDIT — managed by hugoh/go-tools via copier.
---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/go-tools/.github/workflows/semantic-pr.yml@4007d9f75c885810eff858593c3bca12a6fd6cee # v1.26.4
    permissions:
      pull-requests: read
