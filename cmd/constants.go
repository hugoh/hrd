package cmd

const (
	NameLabel   = "NAME"
	VCSLabel    = "VCS"
	RefLabel    = "REF"
	MSGLabel    = "MSG"
	StatusLabel = "STATUS"
)

// These format constants were moved to internal/ui to share between
// CLI and TUI. See ui.FormatSummary().
